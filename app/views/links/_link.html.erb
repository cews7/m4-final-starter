<article id="link<%= link.id %>" class="<%= link.read ? "read" : "" %>"><br>
  Title: <h3><%= link.title %><h3><br>
  URL: <%= link_to link.url, link.url %><br>
  <p>Read? <%= link.read %></p><br>
  <%= button_to "Mark as #{link.read ? "Unread" : "Read"}", link_path(link), method: 'patch', class: "switch", data_id: link.id %>
  <%= button_to "Edit", edit_link_path(link), method: 'get' %>
</article>
